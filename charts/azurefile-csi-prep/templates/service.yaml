apiVersion: v1
kind: Service
metadata:
  name: {{ include "azurefile-csi-prep.fullname" . }}
  labels:
    {{- include "azurefile-csi-prep.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "azurefile-csi-prep.selectorLabels" . | nindent 4 }}
