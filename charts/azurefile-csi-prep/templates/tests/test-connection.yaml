apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "azurefile-csi-prep.fullname" . }}-test-connection"
  labels:
    {{- include "azurefile-csi-prep.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "azurefile-csi-prep.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
